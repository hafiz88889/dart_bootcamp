
   // ?.	Null-aware member access (calls methods or accesses properties only if the object is non-null).
    // ?[]	Null-aware index access (safely access an index in a list or map that might be null).
    // ??	Null-coalescing operator (provides a fallback value if the left-hand expression is null).
    // ??=	Null-coalescing assignment (assigns a value if the variable is null).
   //  ?..	Null-aware cascade (executes a series of operations on an object only if it is non-null).
   // !	Null assertion (forces the compiler to treat a nullable value as non-null).

   void main(){
  String? name;
  print(name);
   }
